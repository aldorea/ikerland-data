---
phase: 03-data-lake-etl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/07-data-lake-etl.md
autonomous: true
requirements:
  - LAKE-01
  - LAKE-02
  - LAKE-03
  - LAKE-05

must_haves:
  truths:
    - "A reader can identify the three S3 medallion zones (Bronze raw JSON, Silver validated Parquet, Gold aggregated Parquet) and understand which Glue ETL step transforms each zone"
    - "A reader can see the Hive-style partition scheme (year=/month=/day=/device_type=/) with a concrete S3 path example"
    - "A reader can see the concrete cost reduction example with dollar amounts: $5 query becomes $0.13 query for one device type, one month"
    - "A reader can see the ETL trigger comparison table (EventBridge Scheduler vs S3 event-driven vs Glue Workflow) with the justified selection of EventBridge Scheduler"
    - "A reader can trace the full cold-path data flow in the Mermaid diagram: Firehose to Bronze to Glue ETL to Silver to Glue ETL to Gold, with Glue Data Catalog as cross-cutting component"
  artifacts:
    - path: "docs/architecture/07-data-lake-etl.md"
      provides: "Data Lake and ETL architecture documentation — medallion zones, cold-path diagram, ETL pipeline, partitioning strategy"
      contains: "## Data Lake"
  key_links:
    - from: "docs/architecture/07-data-lake-etl.md"
      to: "docs/architecture/04-data-pipeline-processing.md"
      via: "Cross-reference to Phase 2 Firehose configuration as the Bronze zone data source"
      pattern: "04-data-pipeline-processing"
---

<objective>
Create the Data Lake and ETL architecture document covering the medallion architecture (Bronze/Silver/Gold), complete cold-path Mermaid diagram, AWS Glue ETL pipeline configuration with PySpark pseudo-code, ETL trigger comparison table, and Hive-style partitioning strategy with concrete cost reduction example.

Purpose: Document the cold-path analytical tier of the IoT platform, complementing Phase 2's hot processing path. This is the bulk of the Phase 3 deliverable — all five content sections that form the foundation of the architecture document.

Output: `docs/architecture/07-data-lake-etl.md` with sections 1 through 5 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-lake-etl/03-CONTEXT.md
@.planning/phases/03-data-lake-etl/03-RESEARCH.md
@docs/architecture/04-data-pipeline-processing.md
@docs/architecture/05-storage-layer.md

<interfaces>
<!-- Phase 2 Firehose output is the input to Phase 3 Bronze zone -->
<!-- Follow the same doc structure pattern as existing architecture docs -->
<!-- Existing docs use: narrative intro -> component table -> Mermaid flowchart LR -> comparison table -> design notes -->
<!-- Comparison tables use columns: Alternative | Pros | Cons | Cost | Recommendation -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Data Lake document with medallion architecture, cold-path diagram, and ETL pipeline</name>
  <files>docs/architecture/07-data-lake-etl.md</files>
  <read_first>
    - docs/architecture/04-data-pipeline-processing.md (Phase 2 Firehose config — Bronze zone data source, established doc format pattern)
    - docs/architecture/05-storage-layer.md (comparison table format reference)
    - .planning/phases/03-data-lake-etl/03-CONTEXT.md (all locked decisions D-01 through D-18)
    - .planning/phases/03-data-lake-etl/03-RESEARCH.md (architecture patterns, pseudo-code, anti-patterns)
  </read_first>
  <action>
Create `docs/architecture/07-data-lake-etl.md` following the established doc pattern (narrative -> table -> Mermaid diagram -> comparison table -> design notes). Include these sections:

**Section 1 — Introduction: Cold-Path Data Lake Overview**
- Narrative explaining the cold path role: Kinesis Data Firehose (Phase 2) delivers raw JSON telemetry to S3 Bronze zone. Phase 3 documents everything from Bronze onward — ETL transformation, analytical storage, and query access.
- Cross-reference: "See `04-data-pipeline-processing.md` for the Firehose delivery stream configuration that feeds the Bronze zone."
- Per D-14: The Data Lake cold path starts where Phase 2's Kinesis Data Firehose ends.

**Section 2 — Medallion Zone Inventory (per D-01, D-02, D-03)**

Include a zone inventory table with these exact rows:

| Zone | S3 Prefix | Format | Partition Keys | Written By | Storage Class | Glue Catalog Table |
|------|-----------|--------|----------------|------------|---------------|--------------------|
| Bronze | `s3://data-lake/bronze/telemetry/year=/month=/day=/` | Raw JSON (NDJSON) | year, month, day | Kinesis Data Firehose (Phase 2) | S3 Intelligent-Tiering | `datalake.bronze_telemetry` |
| Silver | `s3://data-lake/silver/telemetry/year=/month=/day=/device_type=/` | Parquet (Snappy) | year, month, day, device_type | Glue ETL (Bronze->Silver) | S3 Standard | `datalake.silver_telemetry` |
| Gold — Hourly Metrics | `s3://data-lake/gold/hourly_metrics/year=/month=/day=/device_type=/` | Parquet (Snappy) | year, month, day, device_type | Glue ETL (Silver->Gold) | S3 Standard | `datalake.gold_hourly_metrics` |
| Gold — Daily Rollups | `s3://data-lake/gold/daily_rollups/year=/month=/day=/` | Parquet (Snappy) | year, month, day | Glue ETL (Silver->Gold) | S3 Standard | `datalake.gold_daily_rollups` |
| Gold — Device Health | `s3://data-lake/gold/device_health/year=/month=/day=/` | Parquet (Snappy) | year, month, day | Glue ETL (Silver->Gold) | S3 Standard | `datalake.gold_device_health` |

After the table, describe each zone:
- **Bronze**: Immutable landing zone. Never modified after Firehose write. Serves as audit trail and replay source. S3 Intelligent-Tiering auto-moves cold raw data to cheaper tiers after 30 days (per D-02).
- **Silver**: Schema-validated, cleaned Parquet. Transformations include: null handling, timestamp normalization (Unix epoch -> UTC ISO-8601), JSON->Parquet conversion, field type enforcement, deduplication of late-arriving records (per D-01).
- **Gold**: Pre-computed aggregates for dashboard and reporting queries. Three sub-zones: `hourly_metrics/` (avg, min, max per device per hour), `daily_rollups/` (per device-type fleet summary), `device_health/` (last-seen, uptime percentage) (per D-01).

Per D-03: All three zones registered in Glue Data Catalog as separate database tables under the `datalake` database.

**Section 3 — Complete Cold-Path Mermaid Diagram (per D-17)**

Create a `flowchart LR` Mermaid diagram showing:
- Kinesis Data Firehose (labeled as Phase 2 output) -> S3 Bronze
- S3 Bronze -> Glue ETL Job (Bronze->Silver) -> S3 Silver
- S3 Silver -> Glue ETL Job (Silver->Gold) -> S3 Gold (with sub-nodes for hourly_metrics, daily_rollups, device_health)
- S3 Silver -> Amazon Athena (query arrow)
- S3 Gold -> Amazon Athena (query arrow)
- AWS Glue Data Catalog as a cross-cutting component connected to: Firehose (schema), both Glue ETL jobs (read/write catalog), and Athena (table metadata)
- EventBridge Scheduler connected to both Glue ETL jobs (trigger arrows)
- Lake Formation connected to Athena (access control)

Use the same `flowchart LR` style as existing Phase 1-2 docs. Use subgraphs to group related components (e.g., "S3 Data Lake" subgraph containing Bronze/Silver/Gold).

**Section 4 — ETL Pipeline Configuration (per D-04, D-05, D-06, D-07)**

Sub-section 4a — Bronze->Silver Glue Job Configuration. Include this config table:

| Parameter | Value | Rationale |
|-----------|-------|-----------|
| Glue Version | 4.0 | Spark 3.3, Python 3.10. Latest stable. |
| Worker Type | G.1X | 4 vCPU, 16 GB RAM per worker. Sufficient for hourly IoT batches at thousands-of-devices scale. |
| Auto-scaling | Enabled | Glue auto-scaling adjusts worker count dynamically. Avoids over-provisioning for variable batch sizes. |
| IAM Role | `glue-etl-role` | s3:GetObject (bronze prefix), s3:PutObject (silver prefix), glue:UpdateTable (catalog), kms:Decrypt/Encrypt |
| Schedule | Hourly (`0 * * * ? *`) via EventBridge Scheduler | Processes previous hour's Bronze data. |
| Input | Glue Catalog `datalake.bronze_telemetry` | Read via DynamicFrame with push_down_predicate for date partition. |
| Output | Glue Catalog `datalake.silver_telemetry` | Write Parquet (Snappy) with partitionKeys=["year","month","day","device_type"]. |

Include transformation steps as a numbered list (per D-06):
1. Read Bronze JSON via Glue DynamicFrame from `datalake.bronze_telemetry` with push_down_predicate for the target hour
2. Apply schema: cast `ts` (Unix epoch) to UTC ISO-8601 `timestamp`, enforce `temperature`/`humidity` as `double`, drop null `thingName` records
3. Extract `device_type` from DynamoDB device-metadata lookup (or from embedded field if populated at ingest time)
4. Deduplicate late-arriving records: drop records where `(thingName, ts)` already exists in Silver partition
5. Write to Silver with `partitionKeys=["year","month","day","device_type"]` in Parquet/Snappy
6. Register new partitions via explicit `ALTER TABLE datalake.silver_telemetry ADD IF NOT EXISTS PARTITION (...)`

Include PySpark pseudo-code block (per D-06) — use the exact pseudo-code from 03-RESEARCH.md Pattern 3.

Sub-section 4b — Silver->Gold Glue Job. Brief config table (daily schedule `0 2 * * ? *`, reads Silver, writes three Gold sub-zones with aggregation logic: AVG/MIN/MAX per device per hour for hourly_metrics, fleet summary for daily_rollups, last-seen/uptime for device_health).

Sub-section 4c — Partition Registration: Crawler vs Explicit (per D-07). Include this comparison table:

| Approach | Speed | Cost | Risk | Recommendation |
|----------|-------|------|------|----------------|
| Explicit partition registration in ETL job | Immediate (within job) | Zero extra cost | Schema must match exactly | **Recommended** — deterministic, zero DPU overhead |
| Glue Crawler after ETL | Minutes delay | ~$0.44/DPU-hour per crawl | Schema drift possible; crawler overwrites custom types | Acceptable for schema discovery; avoid for scheduled production ETL |

Sub-section 4d — ETL Trigger Comparison Table (per D-05). Include this exact table:

| Trigger Mechanism | Latency | Complexity | Cost | Best For |
|-------------------|---------|------------|------|----------|
| EventBridge Scheduler cron | Up to 1 hour (hourly schedule) | Low — no extra services | $1/million schedule invocations | **Recommended:** predictable, batch-oriented, hourly IoT telemetry |
| S3 Event -> Lambda -> Glue StartJobRun | Minutes after Firehose flush | Medium — Lambda + IAM + event routing | Lambda invocation cost per S3 PUT | When near-real-time transformation is required |
| Glue Workflow + Glue Triggers | Batch-aligned with Glue | Medium — Glue-native but tightly coupled | Included in Glue | When multi-step ETL with dependencies must be visualized in Glue console |

Add narrative explaining why EventBridge Scheduler was selected: decoupled from Glue, timezone-aware cron, built-in retry policy and DLQ for failed job starts, observable via CloudWatch.

**Section 5 — Partitioning Strategy (per D-08, D-09, D-10, D-18)**

Sub-section 5a — Hive-Style Partition Scheme. Explain partition key selection: `device_type` (not `device_id`) because thousands of device-specific partitions create too many small files, degrading query performance and increasing S3 LIST call costs.

Include concrete S3 path examples (per D-18):
```
s3://data-lake/bronze/telemetry/year=2024/month=03/day=15/          (raw JSON from Firehose)
s3://data-lake/silver/telemetry/year=2024/month=03/day=15/device_type=temperature-sensor/part-0001.parquet
s3://data-lake/gold/hourly_metrics/year=2024/month=03/day=15/device_type=temperature-sensor/part-0001.parquet
```

Sub-section 5b — Firehose Dynamic Partitioning (per D-10). Explain that Firehose writes Bronze with `year=/month=/day=/` prefixes. The ETL job adds `device_type=` during Bronze->Silver.

Sub-section 5c — Cost Reduction Example (per D-09). Include this exact cost table:

| Query Scope | Data Scanned | Athena Cost ($5/TB) |
|-------------|-------------|---------------------|
| Full table scan (no partitioning) | 100 GB/month | $0.50 |
| Full table scan at 1 year | 1.2 TB | $6.00 |
| One device_type, one month (partitioned) | 2.5% of monthly = 2.5 GB | $0.013 |
| One device_type, one day (partitioned) | ~83 MB | less than $0.001 |

Include the D-09 dollar illustration verbatim: "Querying one device type for one month scans ~2.5% of data vs 100% for an unpartitioned layout. At $5/TB scanned, this turns a $5.00 query into a $0.13 query."
  </action>
  <verify>
    <automated>grep -c "Bronze\|Silver\|Gold" docs/architecture/07-data-lake-etl.md | awk '{if ($1 >= 15) print "PASS: medallion zones well-documented ("$1" mentions)"; else print "FAIL: insufficient medallion zone coverage ("$1" mentions)"}'</automated>
  </verify>
  <acceptance_criteria>
    - docs/architecture/07-data-lake-etl.md exists and is non-empty
    - File contains "## Data Lake" or "## Cold-Path Data Lake" as a top-level heading
    - File contains "Bronze" at least 10 times (zone inventory, diagram, ETL steps, partitioning)
    - File contains "Silver" at least 10 times
    - File contains "Gold" at least 8 times
    - File contains "```mermaid" (cold-path diagram per D-17)
    - File contains "flowchart LR" (matching established diagram style)
    - File contains "Glue Data Catalog" (cross-cutting component per D-03)
    - File contains "datalake.bronze_telemetry" (Glue Catalog table name per D-03)
    - File contains "datalake.silver_telemetry" (Glue Catalog table name per D-03)
    - File contains "datalake.gold_hourly_metrics" (Glue Catalog table name per D-03)
    - File contains "EventBridge Scheduler" (ETL trigger per D-04)
    - File contains "0 * * * ? *" (hourly cron expression per D-04)
    - File contains "G.1X" (worker type per D-06)
    - File contains "Glue 4.0" or "Glue Version | 4.0" (per D-06)
    - File contains "push_down_predicate" (PySpark pseudo-code per D-06)
    - File contains "partitionKeys" (Glue write config per D-06)
    - File contains "ALTER TABLE" (explicit partition registration per D-07)
    - File contains "Crawler" (comparison with explicit approach per D-07)
    - File contains the ETL trigger comparison table with "EventBridge Scheduler cron" and "S3 Event" and "Glue Workflow"
    - File contains "device_type" partition key (per D-08)
    - File contains "year=2024/month=03/day=15" (concrete S3 path example per D-18)
    - File contains "$5.00 query" or "$5 query" and "$0.13 query" (cost reduction example per D-09)
    - File contains "04-data-pipeline-processing" (Phase 2 cross-reference per D-14)
    - File contains "Intelligent-Tiering" (Bronze storage class per D-02)
  </acceptance_criteria>
  <done>
    The document docs/architecture/07-data-lake-etl.md exists with sections 1-5 complete: introduction with Phase 2 cross-reference, medallion zone inventory table, complete cold-path Mermaid flowchart LR diagram, ETL pipeline configuration with PySpark pseudo-code and two comparison tables (trigger mechanism and partition registration), and partitioning strategy with concrete S3 path examples and dollar-amount cost reduction table.
  </done>
</task>

</tasks>

<verification>
- docs/architecture/07-data-lake-etl.md exists with sections 1-5
- Mermaid diagram renders correctly (flowchart LR syntax)
- All comparison tables have the correct column structure
- PySpark pseudo-code is syntactically valid
- Cross-reference to Phase 2 doc is present
- Cost reduction example includes exact dollar amounts per D-09
</verification>

<success_criteria>
- LAKE-01: Medallion zones (Bronze/Silver/Gold) are documented with zone inventory table and descriptions
- LAKE-02: Glue ETL jobs documented with config tables and PySpark pseudo-code
- LAKE-03: ETL trigger comparison table present with EventBridge Scheduler as justified selection
- LAKE-05: Hive-style partitioning documented with concrete S3 paths and dollar-amount cost example
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-lake-etl/03-01-SUMMARY.md`
</output>
