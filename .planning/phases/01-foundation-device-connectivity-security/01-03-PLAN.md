---
phase: 01-foundation-device-connectivity-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/03-device-management.md
autonomous: true
requirements:
  - DEVM-01
  - DEVM-02
  - DEVM-03
  - DEVM-04

must_haves:
  truths:
    - "A reader can follow the Device Shadow sequence diagram step-by-step: web writes desired → Shadow stores → device reconnects → fetches delta → executes command → updates reported → delta cleared"
    - "A reader can see version-number idempotency in the Device Shadow flow (version N+1 in delta, device checks version > last applied)"
    - "A reader can trace the Fleet Provisioning claim certificate exchange from factory-embedded claim cert to per-device X.509 certificate"
    - "A reader can find the IoT policy JSON with ${iot:ThingName} variable scoping per-device topic access"
    - "A reader can understand Thing Types and Thing Groups hierarchy for fleet organization"
  artifacts:
    - path: "docs/architecture/03-device-management.md"
      provides: "Device management documentation with Shadow sequence diagram, Fleet Provisioning flow, IoT policy, and fleet grouping"
      contains: "sequenceDiagram"
  key_links:
    - from: "docs/architecture/03-device-management.md"
      to: "Device Shadow lifecycle"
      via: "Mermaid sequenceDiagram with 6 actors and version numbers"
      pattern: "sequenceDiagram"
    - from: "docs/architecture/03-device-management.md"
      to: "IoT policy"
      via: "JSON pseudo-policy with iot:ThingName variables"
      pattern: "iot:ThingName"
    - from: "docs/architecture/03-device-management.md"
      to: "Fleet Provisioning"
      via: "Numbered step sequence or sequenceDiagram"
      pattern: "CreateKeysAndCertificate|RegisterThing"
---

<objective>
Write the Device Management section of the IoT architecture document, covering Device Shadow command delivery (with full sequence diagram), X.509 certificate authentication (Fleet Provisioning), IoT policy variable scoping, and Thing Types/Groups fleet organization.

Purpose: This section contains the most evaluator-scrutinized artifacts: the Device Shadow sequence diagram and IoT policy examples. Evaluators check for version-number idempotency, delta subscription, and `${iot:ThingName}` policy variable usage.
Output: `docs/architecture/03-device-management.md` — standalone Markdown file with Mermaid sequence diagram and policy examples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-device-connectivity-security/01-CONTEXT.md
@.planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Device Shadow section with full sequence diagram</name>
  <files>docs/architecture/03-device-management.md</files>
  <read_first>
    .planning/phases/01-foundation-device-connectivity-security/01-CONTEXT.md
    .planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
  </read_first>
  <action>
Create the file `docs/architecture/03-device-management.md` with the following sections:

**1. Section heading:** `## Device Management` with intro paragraph: This section documents how the platform manages IoT devices — command delivery via Device Shadow, certificate-based authentication via Fleet Provisioning, per-device authorization via IoT policy variables, and fleet organization via Thing Types and Thing Groups.

**2. Device Shadow — Command Delivery (`### Device Shadow — Command Delivery to Disconnected Devices`):**
- Opening paragraph (per DEVM-01): Devices in this architecture connect hourly for data push, meaning they are offline most of the time. AWS IoT Core Device Shadow provides a persistent cloud-side state document for each device, containing `desired` (what the cloud wants), `reported` (what the device confirmed), and a computed `delta` (the difference). When an operator sends a command, the cloud writes to `desired`; when the device reconnects, it receives the `delta` containing only changed fields, applies them, and updates `reported` to confirm. The shadow reconciles automatically when `desired` equals `reported`.
- **Key concepts table:**
  | Concept | Description |
  |---|---|
  | `desired` state | Set by cloud (Lambda) — represents the target configuration or command |
  | `reported` state | Set by device — represents the actual device configuration after applying commands |
  | `delta` state | Computed by IoT Core — contains fields where `desired` differs from `reported` |
  | `version` | Monotonically increasing integer — used for optimistic locking and idempotency |
  | Named shadow | Each device can have multiple named shadows for different concerns (e.g., `config`, `firmware`) |

- **Full Mermaid sequence diagram (per DEVM-02)** — this is the most evaluator-scrutinized diagram in the document. Use the EXACT pattern from 01-RESEARCH.md Pattern 3 (Device Shadow Sequence Diagram), including ALL of the following interactions:

  Participants (6 actors):
  1. `WebUI` as `Web UI (Operator)`
  2. `APIGW` as `API Gateway`
  3. `LambdaCmd` as `Lambda (Command Handler)`
  4. `Shadow` as `IoT Core Shadow`
  5. `Dev` as `IoT Device (hourly connect)`
  6. `Audit` as `DynamoDB (Audit)`

  Sequence (must include ALL of these steps):
  1. WebUI sends POST /devices/{thingName}/commands to API Gateway with payload `{command: "setSampleRate", value: 300}`
  2. API Gateway invokes Lambda
  3. Lambda calls `UpdateThingShadow` with `{desired: {sampleRate: 300}}`
  4. Lambda writes audit record to DynamoDB with status=PENDING
  5. Shadow returns accepted with `version: N+1` (show concrete number like 15)
  6. `rect` block highlighting offline period: "Device is OFFLINE — desired state persisted in Shadow (version 15)"
  7. Device connects via MQTT TLS 8883 with per-device X.509 cert
  8. Device subscribes to `$aws/things/{thingName}/shadow/update/delta`
  9. Shadow sends delta: `{state: {sampleRate: 300}, version: 15}`
  10. `Note over Dev`: "version 15 > last applied (12) — apply command" (idempotency guard)
  11. Device applies sampleRate = 300 (use activate/deactivate blocks)
  12. Device publishes shadow update: `{reported: {sampleRate: 300}, version: 15}`
  13. Shadow responds: update/accepted (delta cleared)
  14. Shadow triggers IoT Rule on shadow update
  15. Lambda updates DynamoDB audit: status=DELIVERED

  CRITICAL: Include version numbers (e.g., version: 15) in the delta message and the Note block about idempotency. Without version numbers, evaluators consider the diagram incomplete.

- After the diagram, add a **Version-Based Idempotency** subsection explaining:
  - Every shadow update increments the version number
  - The device stores the last successfully applied version locally
  - When a delta arrives, the device compares the received version to its last applied version
  - If received version <= last applied, the delta is discarded (prevents double-execution from retransmissions)
  - If the device sends an update with a stale version, IoT Core returns HTTP 409 Conflict

- Add a **Shadow Comparison Table** — Device Shadow vs alternatives (per evaluator expectations):
  | Approach | Offline Support | Complexity | AWS Integration | Recommendation |
  |---|---|---|---|---|
  | Device Shadow (desired/reported/delta) | Built-in — persists while device is offline | Low — managed by IoT Core | Native — Rules Engine, Lambda triggers | **Recommended** |
  | SQS queue per device | Yes — messages retained 14 days | High — custom polling logic, per-device queues | Manual — must poll from device or proxy | Not recommended for this use case |
  | DynamoDB polling | Yes — records persist indefinitely | High — custom poll + mark-as-read logic | Manual — device or proxy must query | Not recommended — polling waste |
  </action>
  <verify>
    <automated>grep -q "sequenceDiagram" docs/architecture/03-device-management.md && grep -q "version: 15" docs/architecture/03-device-management.md && grep -q "delta" docs/architecture/03-device-management.md && grep -q "OFFLINE" docs/architecture/03-device-management.md && grep -q "idempotency" docs/architecture/03-device-management.md && grep -q "UpdateThingShadow" docs/architecture/03-device-management.md && grep -q "DELIVERED" docs/architecture/03-device-management.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/architecture/03-device-management.md` exists
    - File contains `sequenceDiagram` Mermaid block
    - Diagram contains all 6 participants: WebUI, APIGW/API, LambdaCmd/Lambda, Shadow, Dev/Device, Audit/DDB
    - Diagram contains `version: 15` (or similar concrete version number) in the delta message
    - Diagram contains `rect` block for offline period with text mentioning "OFFLINE"
    - Diagram contains `UpdateThingShadow` API call
    - Diagram contains `$aws/things` shadow topic subscription
    - Diagram contains both `PENDING` and `DELIVERED` audit statuses
    - File contains `Note over` block mentioning version comparison for idempotency
    - File contains comparison table with `| Approach |` header (Shadow vs SQS vs DynamoDB polling)
    - File contains `activate` and `deactivate` Mermaid keywords for device processing
  </acceptance_criteria>
  <done>Device Shadow section complete with full 15-step sequence diagram showing all 6 actors, version-number idempotency (version 15 in delta, Note block comparing to last applied), offline period highlighting, and comparison table against SQS and DynamoDB alternatives.</done>
</task>

<task type="auto">
  <name>Task 2: Write Fleet Provisioning, IoT policy, and Thing Types/Groups sections</name>
  <files>docs/architecture/03-device-management.md</files>
  <read_first>
    docs/architecture/03-device-management.md
    .planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
  </read_first>
  <action>
Append the following sections to `docs/architecture/03-device-management.md` (after the Device Shadow content from Task 1):

**1. X.509 Certificate Authentication — Fleet Provisioning (`### X.509 Certificate Authentication`):**
- Opening paragraph (per DEVM-03, D-05): Every device authenticates to IoT Core using a unique X.509 certificate. Devices are provisioned at scale using Fleet Provisioning by Claim — a factory-embedded shared claim certificate is exchanged for a per-device certificate on first connection. This eliminates the need for manual certificate installation on each device.
- **Fleet Provisioning Sequence** — Use the EXACT 10-step sequence from 01-RESEARCH.md Pattern 5. Present as a Mermaid `sequenceDiagram` with participants: Factory, Device, IoTCore, ProvisioningLambda (pre-hook), DynamoDB (allowlist). Steps:
  1. Factory embeds claim certificate + restrictive IoT policy on device
  2. Device boots, connects to IoT Core using claim cert over TLS 1.2
  3. Device calls `CreateKeysAndCertificate` (or `CreateCertificateFromCsr` for CSR-based provisioning where private key never leaves device)
  4. IoT Core returns: new certificate + private key + `certificateOwnershipToken` (1-hour expiry)
  5. Device calls `RegisterThing` with token + provisioning template name
  6. Provisioning Lambda (pre-hook) validates device serial against DynamoDB allowlist
  7. Fleet Provisioning creates: IoT Thing, attaches per-device cert, assigns Thing Groups, sets Thing Type
  8. Device saves per-device cert to secure storage
  9. Device disconnects claim cert session, reconnects with new per-device cert
  10. Claim cert can be revoked per batch without affecting provisioned devices
- Note (per D-07): Certificate rotation (replacing per-device certs before expiry) is a v2 design consideration — documented as a future enhancement, not part of the initial architecture.
- Note: "Claim certificates should be unique per manufacturing batch. If a claim cert is compromised, only that batch needs revocation — provisioned devices are unaffected."

**2. IoT Policy — Per-Device Topic Authorization (`### IoT Policy — Per-Device Authorization`):**
- Opening paragraph (per D-06, DEVM-03): Each device's X.509 certificate has an attached IoT policy that uses `${iot:ThingName}` policy variables to restrict the device to its own topic namespace. A single policy template is reused across all devices — the variable substitution happens at runtime when the device connects.
- **Full pseudo-policy JSON block** — Use the EXACT pattern from 01-RESEARCH.md Pattern 4:
  ```json
  {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Effect": "Allow",
        "Action": "iot:Connect",
        "Resource": "arn:aws:iot:{region}:{accountId}:client/${iot:ClientId}"
      },
      {
        "Effect": "Allow",
        "Action": "iot:Publish",
        "Resource": [
          "arn:aws:iot:{region}:{accountId}:topic/devices/${iot:ThingName}/telemetry",
          "arn:aws:iot:{region}:{accountId}:topic/devices/${iot:ThingName}/alarm",
          "arn:aws:iot:{region}:{accountId}:topic/devices/${iot:ThingName}/config"
        ]
      },
      {
        "Effect": "Allow",
        "Action": ["iot:Subscribe", "iot:Receive"],
        "Resource": [
          "arn:aws:iot:{region}:{accountId}:topicfilter/$aws/things/${iot:ThingName}/shadow/update/delta",
          "arn:aws:iot:{region}:{accountId}:topicfilter/$aws/things/${iot:ThingName}/shadow/get/accepted"
        ]
      }
    ]
  }
  ```
- After the JSON block, add an explanation paragraph: "This policy ensures that device `sensor-001` can only publish to `devices/sensor-001/telemetry`, `devices/sensor-001/alarm`, and `devices/sensor-001/config`. It cannot publish to any other device's topics. The `iot:Subscribe` and `iot:Receive` actions are restricted to the device's own shadow delta and get/accepted topics."
- **Security anti-pattern callout** (use a blockquote or bold warning): "**Security Anti-Pattern:** A policy with `iot:*` on `arn:aws:iot:*:*:*` means any compromised device certificate can publish to any other device's topic, read other devices' shadow state, and potentially issue commands. Always use `${iot:ThingName}` scoping."
- Provisioning comparison table:
  | Method | Scale | Automation | Security | Recommendation |
  |---|---|---|---|---|
  | Fleet Provisioning by Claim | High — thousands of devices | Full — zero-touch after factory | Claim cert per batch; per-device cert issued automatically | **Recommended** |
  | Just-in-Time Registration (JITR) | Medium | Semi — requires custom CA + Lambda | Custom CA required; more control but more complexity | Alternative for custom CA scenarios |
  | Manual provisioning (console/CLI) | Low — tens of devices | None — per-device manual steps | Direct cert issuance — simple but unscalable | Dev/test only |

**3. Thing Types and Thing Groups (`### Device Fleet Organization — Thing Types and Thing Groups`):**
- Opening paragraph (per DEVM-04): The IoT device registry organizes devices using Thing Types (shared attribute schemas) and Thing Groups (hierarchical grouping for policy and management).
- **Thing Types table:**
  | Thing Type | Attributes | Use Case |
  |---|---|---|
  | `SensorDevice` | `firmwareVersion`, `hardwareRevision`, `manufacturer`, `model` | Standard sensor devices (temperature, humidity, pressure) |
  | `GatewayDevice` | `firmwareVersion`, `hardwareRevision`, `manufacturer`, `connectedSensorCount` | Gateway devices aggregating multiple sensors |
- **Thing Groups hierarchy** — Use a code block tree structure from 01-RESEARCH.md Pattern 8:
  ```
  Root
  └── AllDevices (group)
      ├── TemperatureSensors (group) — Thing Type: SensorDevice
      │   ├── Factory-A-TempSensors (subgroup)
      │   └── Factory-B-TempSensors (subgroup)
      ├── PressureSensors (group) — Thing Type: SensorDevice
      └── Gateways (group) — Thing Type: GatewayDevice
  ```
- Explain: "Policies attached to a Thing Group apply to all member devices. This enables fleet-wide configuration updates — update the group policy rather than modifying each device individually. A device can belong to multiple groups. Child groups inherit parent group policies."
- Explain dynamic groups: "Dynamic Thing Groups use fleet indexing queries (e.g., `attributes.firmwareVersion < '2.0'`) to automatically include devices matching criteria. This is useful for targeted firmware updates or monitoring devices with specific attributes."
  </action>
  <verify>
    <automated>grep -q "CreateKeysAndCertificate" docs/architecture/03-device-management.md && grep -q "RegisterThing" docs/architecture/03-device-management.md && grep -q 'iot:ThingName' docs/architecture/03-device-management.md && grep -q "iot:Connect" docs/architecture/03-device-management.md && grep -q "iot:Publish" docs/architecture/03-device-management.md && grep -q "SensorDevice" docs/architecture/03-device-management.md && grep -q "GatewayDevice" docs/architecture/03-device-management.md && grep -q "AllDevices" docs/architecture/03-device-management.md && grep -q "Anti-Pattern" docs/architecture/03-device-management.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `CreateKeysAndCertificate` and `RegisterThing` Fleet Provisioning API calls
    - File contains `certificateOwnershipToken` in the provisioning sequence
    - File contains full IoT policy JSON with `${iot:ThingName}` variable (at least 5 occurrences of `iot:ThingName`)
    - File contains `iot:Connect`, `iot:Publish`, `iot:Subscribe`, `iot:Receive` policy actions
    - File contains security anti-pattern warning mentioning `iot:*` wildcard danger
    - File contains provisioning comparison table with Fleet Provisioning, JITR, and Manual rows
    - File contains `SensorDevice` and `GatewayDevice` Thing Types
    - File contains `AllDevices` group hierarchy tree
    - File contains dynamic groups mention with `attributes.firmwareVersion` query example
    - File contains certificate rotation noted as v2 (per D-07)
  </acceptance_criteria>
  <done>Fleet Provisioning documented with 10-step sequence (claim cert exchange → per-device cert), IoT policy JSON with ${iot:ThingName} scoping (4 actions, security anti-pattern warning), provisioning comparison table (3 methods), Thing Types (2 types with attributes), and Thing Groups hierarchy with dynamic groups explanation.</done>
</task>

</tasks>

<verification>
- `docs/architecture/03-device-management.md` exists and is valid Markdown
- All 4 DEVM requirements are addressed:
  - DEVM-01: Device Shadow desired/reported/delta flow (sequence diagram + concepts table)
  - DEVM-02: Full command delivery lifecycle sequence diagram (15 steps, 6 actors, version numbers)
  - DEVM-03: X.509 authentication with IoT policy variables (Fleet Provisioning + policy JSON)
  - DEVM-04: Thing Types and Thing Groups (type table + group hierarchy + dynamic groups)
</verification>

<success_criteria>
- An evaluator can follow the Device Shadow sequence diagram step-by-step and verify: version numbers present, offline period shown, delta subscription explicit, idempotency guard documented.
- An evaluator can find the IoT policy JSON with `${iot:ThingName}` and verify it restricts each device to its own topic namespace.
- An evaluator can trace the Fleet Provisioning flow from factory claim cert to per-device cert.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-device-connectivity-security/01-03-SUMMARY.md`
</output>
