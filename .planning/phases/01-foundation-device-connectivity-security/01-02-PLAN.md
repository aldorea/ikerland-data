---
phase: 01-foundation-device-connectivity-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/02-device-connectivity-ingestion.md
autonomous: true
requirements:
  - INGT-01
  - INGT-02
  - INGT-03
  - INGT-04

must_haves:
  truths:
    - "A reader can identify IoT Core as the MQTT/HTTPS entry point with protocol details (port 8883 for MQTT, 443 for HTTPS)"
    - "A reader can see the complete topic namespace with specific topic patterns for telemetry, alarm, and config"
    - "A reader can trace each IoT Rules Engine SQL expression to its downstream target and error action"
    - "A reader can understand how Kinesis Data Firehose buffers telemetry for cost-optimized batch processing"
  artifacts:
    - path: "docs/architecture/02-device-connectivity-ingestion.md"
      provides: "Device connectivity and ingestion layer documentation"
      contains: "sequenceDiagram"
  key_links:
    - from: "docs/architecture/02-device-connectivity-ingestion.md"
      to: "Topic namespace"
      via: "Topic table with exact patterns"
      pattern: "devices/\\{thingName\\}/telemetry"
    - from: "docs/architecture/02-device-connectivity-ingestion.md"
      to: "Rules Engine routing"
      via: "Rules inventory table with SQL, action, errorAction"
      pattern: "SELECT.*FROM.*devices"
    - from: "docs/architecture/02-device-connectivity-ingestion.md"
      to: "Kinesis ingestion"
      via: "Data flow diagram showing IoT Core -> Kinesis -> Firehose -> S3"
      pattern: "Kinesis Data Firehose"
---

<objective>
Write the Device Connectivity and Ingestion Layer section of the IoT architecture document, covering IoT Core as the MQTT entry point, topic namespace design, Rules Engine routing, and Kinesis Firehose ingestion buffer.

Purpose: This section documents how devices connect, what topics they publish to, how messages are routed by type, and how telemetry flows into the processing pipeline. Evaluators verify topic namespace correctness, Rules Engine SQL precision, and the presence of error handling (DLQ).
Output: `docs/architecture/02-device-connectivity-ingestion.md` — standalone Markdown file with Mermaid diagrams and tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-device-connectivity-security/01-CONTEXT.md
@.planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write IoT Core entry point, topic namespace, and ingestion data flow diagram</name>
  <files>docs/architecture/02-device-connectivity-ingestion.md</files>
  <read_first>
    .planning/phases/01-foundation-device-connectivity-security/01-CONTEXT.md
    .planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
  </read_first>
  <action>
Create the file `docs/architecture/02-device-connectivity-ingestion.md` with the following sections:

**1. Section heading:** `## Device Connectivity and Ingestion Layer` with intro paragraph: This section documents how IoT devices connect to the cloud platform, the MQTT topic namespace they publish to, how the IoT Rules Engine routes messages by type, and how telemetry flows through Kinesis for cost-optimized batch processing.

**2. IoT Core Entry Point (`### AWS IoT Core — Device Entry Point`):**
- Paragraph (per INGT-01): AWS IoT Core is the managed MQTT broker and HTTPS endpoint for all device communication. Devices publish JSON-formatted telemetry, alarm, and configuration messages over MQTT (port 8883, mutual TLS) or HTTPS (port 443). IoT Core handles TLS termination, certificate-based authentication, topic-level authorization, and message routing via the built-in Rules Engine.
- Protocol support table:
  | Protocol | Port | Authentication | Use Case |
  |---|---|---|---|
  | MQTT | 8883 | X.509 client certificate (mutual TLS) | Primary device communication — persistent connection, bidirectional |
  | MQTT over WebSocket | 443 | SigV4 or Cognito token | Browser-based device simulators or dashboards |
  | HTTPS | 443 | X.509 or SigV4 | One-shot publish for constrained devices without persistent connections |
- Note on Basic Ingest: For high-volume telemetry, devices can publish to reserved topics (`$aws/rules/{ruleName}/devices/{thingName}/telemetry`) to bypass the pub/sub broker and reduce IoT Core messaging costs by approximately 50%. This is recommended when devices send data one-way without needing to receive messages on the same topic.

**3. Topic Namespace Design (`### Topic Namespace`):**
- Paragraph (per D-08, INGT-04): The topic hierarchy follows a `devices/{thingName}/{messageType}` pattern. Each device publishes exclusively to its own namespace, enforced by IoT policies using `${iot:ThingName}` variable substitution.
- Topic namespace table:
  | Topic Pattern | Message Type | Direction | Payload Example | Consumer |
  |---|---|---|---|---|
  | `devices/{thingName}/telemetry` | Telemetry | Device → Cloud | `{"temperature": 23.5, "humidity": 67, "ts": 1711500000}` | Rules Engine → Kinesis Data Streams |
  | `devices/{thingName}/alarm` | Alarm | Device → Cloud | `{"type": "threshold", "metric": "temperature", "value": 85.2, "ts": 1711500000}` | Rules Engine → Lambda (alarm evaluator) |
  | `devices/{thingName}/config` | Config ACK | Device → Cloud | `{"configVersion": 12, "applied": true, "ts": 1711500000}` | Rules Engine → DynamoDB (config table) |
  | `$aws/things/{thingName}/shadow/update` | Shadow update | Device → Cloud | `{"state": {"reported": {"sampleRate": 300}}}` | IoT Core Shadow service (managed) |
  | `$aws/things/{thingName}/shadow/update/delta` | Shadow delta | Cloud → Device | `{"state": {"sampleRate": 300}, "version": 15}` | Device subscribes on connect |
  | `$aws/things/{thingName}/shadow/get` | Shadow get | Device → Cloud | (empty payload) | IoT Core Shadow service (managed) |
  | `$aws/things/{thingName}/shadow/get/accepted` | Shadow response | Cloud → Device | Full shadow document | Device receives current state |
- Note: Shadow topics (`$aws/...`) are managed by IoT Core and do not require Rules Engine configuration.

**4. Ingestion Data Flow (`### Ingestion Data Flow`):**
- Mermaid `flowchart LR` diagram showing the complete message routing from device to downstream targets:
  ```
  flowchart LR
      DEV[IoT Device] -->|MQTT 8883\nmTLS| IOT[AWS IoT Core]
      IOT -->|Rules Engine| RE{Message Router}
      RE -->|"SELECT * FROM\n'devices/+/telemetry'"| KDS[Kinesis Data Streams]
      RE -->|"SELECT * FROM\n'devices/+/alarm'"| LAM_ALARM[Lambda\nAlarm Evaluator]
      RE -->|"SELECT * FROM\n'devices/+/config'"| DDB_CFG[DynamoDB\nConfig Table]
      RE -->|errorAction\nall rules| SQS_DLQ[SQS Dead Letter Queue]
      KDS --> KDF[Kinesis Data Firehose]
      KDS --> LAM_PROC[Lambda\nBatch Processor]
      KDF -->|Parquet conversion| S3[S3 Data Lake\nBronze Zone]
      LAM_PROC --> TS[Amazon Timestream]
      LAM_PROC --> DDB_META[DynamoDB\nDevice Metadata]
  ```
  Adapt the diagram to render correctly in Mermaid (proper node IDs, edge labels, line breaks with `<br/>` if needed instead of `\n`). Ensure the diagram shows: device → IoT Core → Rules Engine fan-out to 3 targets + DLQ → Kinesis splits to Firehose (S3) and Lambda (Timestream/DynamoDB).
  </action>
  <verify>
    <automated>grep -q "flowchart LR" docs/architecture/02-device-connectivity-ingestion.md && grep -q "devices/{thingName}/telemetry" docs/architecture/02-device-connectivity-ingestion.md && grep -q "port 8883" docs/architecture/02-device-connectivity-ingestion.md && grep -q "Basic Ingest" docs/architecture/02-device-connectivity-ingestion.md && grep -q '\\$aws/things' docs/architecture/02-device-connectivity-ingestion.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/architecture/02-device-connectivity-ingestion.md` exists
    - File contains `flowchart LR` Mermaid diagram
    - File contains `devices/{thingName}/telemetry` topic pattern
    - File contains `devices/{thingName}/alarm` topic pattern
    - File contains `devices/{thingName}/config` topic pattern
    - File contains `$aws/things/{thingName}/shadow/update/delta` topic
    - File contains `port 8883` reference
    - File contains `Basic Ingest` reference with cost savings note
    - File contains `| Topic Pattern |` table header
    - Diagram shows `SQS` or `DLQ` node for error handling
  </acceptance_criteria>
  <done>IoT Core entry point documented with protocol table, topic namespace documented with 7-row table covering all custom and shadow topics, and ingestion data flow diagram shows complete device-to-downstream routing.</done>
</task>

<task type="auto">
  <name>Task 2: Write Rules Engine routing table and Kinesis ingestion buffer section</name>
  <files>docs/architecture/02-device-connectivity-ingestion.md</files>
  <read_first>
    docs/architecture/02-device-connectivity-ingestion.md
    .planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
  </read_first>
  <action>
Append the following sections to `docs/architecture/02-device-connectivity-ingestion.md` (after the ingestion data flow from Task 1):

**1. IoT Rules Engine Routing (`### IoT Rules Engine — Message Routing`):**
- Opening paragraph (per D-09, INGT-02): The IoT Rules Engine uses SQL-based rules to inspect each incoming MQTT message and route it to the appropriate downstream AWS service. Each rule targets a specific topic pattern using `topic(2)` function to extract the `thingName` from the topic hierarchy. Per D-10, every rule includes an `errorAction` that routes failed deliveries to an SQS dead-letter queue (DLQ) to prevent silent message loss.
- Rules inventory table using the exact pattern from 01-RESEARCH.md Pattern 2:
  | Rule Name | SQL Statement | Primary Action | Target | errorAction |
  |---|---|---|---|---|
  | TelemetryRule | `SELECT *, topic(2) AS thingName FROM 'devices/+/telemetry'` | Kinesis `putRecord` | Kinesis Data Streams (telemetry stream) | SQS DLQ (`iot-rules-dlq`) |
  | AlarmRule | `SELECT *, topic(2) AS thingName, timestamp() AS ruleTimestamp FROM 'devices/+/alarm'` | Lambda `invoke` | Lambda function (alarm evaluator) | SQS DLQ (`iot-rules-dlq`) |
  | ConfigRule | `SELECT *, topic(2) AS thingName FROM 'devices/+/config'` | DynamoDB `putItem` | DynamoDB table (device-config) | SQS DLQ (`iot-rules-dlq`) |
- Note: The `topic(2)` function extracts the second segment of the topic path (the `thingName`). The `+` wildcard matches any single-level topic segment, so `devices/+/telemetry` matches `devices/sensor-001/telemetry`, `devices/gateway-A/telemetry`, etc.
- Note on partition key strategy: TelemetryRule uses `thingName` as the Kinesis partition key, ensuring all records from the same device land on the same shard for ordered processing.

**2. Kinesis Ingestion Buffer (`### Kinesis Data Streams — Telemetry Buffer`):**
- Paragraph (per INGT-03): Kinesis Data Streams serves as the ingestion buffer between IoT Core and downstream processors. This decouples ingestion rate from processing rate, provides 24-hour replay capability, and enables fan-out to multiple consumers (Lambda batch processor + Kinesis Firehose for S3 delivery).
- Why Kinesis over direct Lambda invocation — comparison table (per INGT-03, evaluator checkpoint):
  | Approach | Cost at Scale | Replay | Ordering | Backpressure | Recommendation |
  |---|---|---|---|---|---|
  | IoT Rules → Lambda (direct) | High — one invocation per message | None — lost if Lambda fails | None guaranteed | None — Lambda throttles, messages drop | Not recommended for telemetry |
  | IoT Rules → SQS → Lambda | Medium — SQS + Lambda costs | Limited (message retention 4-14 days) | FIFO available but expensive | SQS manages — Lambda polls | Acceptable for command queues |
  | IoT Rules → Kinesis → Lambda | Low — batch processing amortizes cost | Yes — 24h-7d retention, replay from any position | Per-shard ordering by partition key | Kinesis buffers — Lambda reads at its own pace | **Recommended for telemetry hot path** |
- Kinesis configuration details:
  - **Mode:** On-Demand (auto-scales shards, no capacity planning)
  - **Retention:** 24 hours (default; extend to 7 days if replay needed for debugging)
  - **Partition key:** `thingName` (ensures per-device ordering within a shard)
  - **Consumers:**
    1. Lambda batch processor (batch size 100-500 records, processes telemetry into Timestream and DynamoDB)
    2. Kinesis Data Firehose delivery stream (buffers and delivers raw JSON to S3 bronze zone, with optional Parquet conversion via Glue Data Catalog)

**3. Kinesis Data Firehose — S3 Delivery (`### Kinesis Data Firehose — S3 Delivery`):**
- Paragraph (per INGT-03): Kinesis Data Firehose provides managed delivery of raw telemetry to the S3 Data Lake bronze zone. Firehose handles batching, compression, and optional format conversion (JSON → Parquet) without custom code.
- Configuration:
  - **Source:** Kinesis Data Streams (telemetry stream)
  - **Destination:** S3 bucket (`s3://iot-datalake-{account}/bronze/telemetry/`)
  - **Buffer:** 60 seconds or 1 MB (whichever comes first)
  - **Format:** Raw JSON (bronze zone); Parquet conversion available via Glue Data Catalog schema
  - **Partitioning:** Dynamic partitioning by `year=/month=/day=/` using Firehose's JQ expressions on the timestamp field
  - **Compression:** GZIP for JSON, Snappy for Parquet
  - **Error handling:** Failed records → S3 error prefix (`s3://iot-datalake-{account}/errors/firehose/`)
  </action>
  <verify>
    <automated>grep -q "TelemetryRule" docs/architecture/02-device-connectivity-ingestion.md && grep -q "AlarmRule" docs/architecture/02-device-connectivity-ingestion.md && grep -q "ConfigRule" docs/architecture/02-device-connectivity-ingestion.md && grep -q "errorAction" docs/architecture/02-device-connectivity-ingestion.md && grep -q "topic(2)" docs/architecture/02-device-connectivity-ingestion.md && grep -q "Kinesis Data Firehose" docs/architecture/02-device-connectivity-ingestion.md && grep -q "Recommended for telemetry" docs/architecture/02-device-connectivity-ingestion.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `TelemetryRule`, `AlarmRule`, `ConfigRule` rule names
    - File contains `SELECT *, topic(2) AS thingName FROM 'devices/+/telemetry'` SQL statement
    - File contains `errorAction` at least 3 times (once per rule)
    - File contains `SQS DLQ` or `iot-rules-dlq` reference
    - File contains `topic(2)` function explanation
    - File contains comparison table with `| Approach |` header comparing Kinesis vs direct Lambda vs SQS
    - File contains "Recommended for telemetry hot path" recommendation
    - File contains `Kinesis Data Firehose` section with S3 delivery configuration
    - File contains `dynamic partitioning` or `year=/month=/day=` partitioning scheme
    - File contains `iot-rules-dlq` DLQ queue name
  </acceptance_criteria>
  <done>Rules Engine routing documented with 3-rule inventory table (SQL + target + errorAction), Kinesis vs alternatives comparison table with clear recommendation, Kinesis configuration details (On-Demand, partition key, consumers), and Firehose S3 delivery with partitioning scheme.</done>
</task>

</tasks>

<verification>
- `docs/architecture/02-device-connectivity-ingestion.md` exists and is valid Markdown
- All 4 INGT requirements are addressed:
  - INGT-01: IoT Core as MQTT/HTTPS entry point (protocol table, port 8883)
  - INGT-02: Rules Engine routing with SQL expressions (3-rule inventory table)
  - INGT-03: Kinesis Firehose as ingestion buffer (comparison table + configuration)
  - INGT-04: Topic namespace design (7-row topic table with patterns and payload examples)
</verification>

<success_criteria>
- An evaluator can trace the complete path from device MQTT publish through IoT Core, Rules Engine SQL, to the appropriate downstream target (Kinesis/Lambda/DynamoDB).
- An evaluator can see why Kinesis was chosen over direct Lambda, supported by a comparison table.
- Every rule has a visible errorAction — no silent message loss paths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-device-connectivity-security/01-02-SUMMARY.md`
</output>
