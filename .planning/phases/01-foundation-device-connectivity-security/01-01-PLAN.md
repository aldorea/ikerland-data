---
phase: 01-foundation-device-connectivity-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/01-security-foundation.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
  - SEC-06

must_haves:
  truths:
    - "A reader can identify every subnet tier (public/private/data) from the VPC diagram, including which services sit in which subnet"
    - "A reader can see VPC endpoint types explicitly labeled as Gateway or Interface for each service"
    - "A reader can find an IAM roles inventory table listing every role, its principal, permissions, and scope"
    - "A reader can verify encryption at rest (KMS CMK per service) and encryption in transit (TLS 1.2+ enforced by IoT Core)"
    - "A reader can identify WAF WebACL attachment points on both CloudFront and API Gateway"
  artifacts:
    - path: "docs/architecture/01-security-foundation.md"
      provides: "Complete security foundation documentation section"
      contains: "flowchart TD"
  key_links:
    - from: "docs/architecture/01-security-foundation.md"
      to: "VPC diagram"
      via: "Mermaid flowchart TD with subgraph blocks for each tier and AZ"
      pattern: "subgraph.*Public Subnet|subgraph.*Private Subnet|subgraph.*Data Subnet"
    - from: "docs/architecture/01-security-foundation.md"
      to: "VPC endpoints"
      via: "Explicit Gateway/Interface labels in diagram and prose"
      pattern: "Gateway endpoint|Interface endpoint"
    - from: "docs/architecture/01-security-foundation.md"
      to: "IAM roles table"
      via: "Markdown table with Role Name, Assumed By, Permissions, Scope columns"
      pattern: "\\| Role Name \\|"
---

<objective>
Write the Security Foundation section of the IoT architecture document, covering VPC topology, IAM least-privilege roles, KMS encryption at rest, TLS encryption in transit, and WAF placement.

Purpose: This section establishes the security baseline that all subsequent architecture layers reference. Evaluators use it to verify the candidate understands AWS network isolation, identity management, and encryption.
Output: `docs/architecture/01-security-foundation.md` — a standalone Markdown file with Mermaid diagrams and tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-device-connectivity-security/01-CONTEXT.md
@.planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write VPC topology section with Mermaid diagram and security groups table</name>
  <files>docs/architecture/01-security-foundation.md</files>
  <read_first>
    .planning/phases/01-foundation-device-connectivity-security/01-CONTEXT.md
    .planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
  </read_first>
  <action>
Create the file `docs/architecture/01-security-foundation.md` with the following sections:

**1. Section heading:** `## Security Foundation` with a brief intro paragraph explaining this section covers VPC isolation, IAM, encryption, and WAF as cross-cutting security concerns.

**2. VPC Topology subsection (`### VPC Topology`):**
- Opening paragraph: 3-tier VPC design (per D-01) across 2 AZs (per D-02), single NAT Gateway for cost optimization (per D-03) with a note that production HA would use one NAT per AZ.
- CIDR block: `10.0.0.0/16`
- Subnet table listing all 6 subnets with columns: Subnet Name, AZ, CIDR, Tier, Services Hosted. Use these exact values:
  | Subnet Name | AZ | CIDR | Tier | Services Hosted |
  |---|---|---|---|---|
  | Public Subnet AZ-1 | us-east-1a | 10.0.1.0/24 | Public | NAT Gateway, ALB |
  | Public Subnet AZ-2 | us-east-1b | 10.0.2.0/24 | Public | (NAT Gateway standby — multi-NAT HA option) |
  | Private Subnet AZ-1 | us-east-1a | 10.0.3.0/24 | Private | Lambda Functions, API Gateway VPC Link |
  | Private Subnet AZ-2 | us-east-1b | 10.0.4.0/24 | Private | Lambda Functions |
  | Data Subnet AZ-1 | us-east-1a | 10.0.5.0/24 | Data | Aurora Primary, Timestream (Interface endpoint), DynamoDB (Gateway endpoint) |
  | Data Subnet AZ-2 | us-east-1b | 10.0.6.0/24 | Data | Aurora Standby |

- Mermaid `flowchart TD` diagram using the exact pattern from 01-RESEARCH.md Pattern 1 (VPC Flowchart). The diagram MUST:
  - Use nested `subgraph` blocks for VPC > Tier > AZ
  - Label every subgraph with tier name AND AZ (e.g., `"Public Subnet AZ-1 (10.0.1.0/24)"`)
  - Show Internet Gateway connecting to public subnets
  - Show NAT route from public to private tier
  - Show private to data tier connections
  - Label VPC endpoints inline: `"Timestream\n(Interface endpoint)"`, `"DynamoDB\n(Gateway endpoint)"`
  - Include S3 Gateway endpoint node in data tier

**3. VPC Endpoints subsection (`### VPC Endpoints`):**
- Paragraph explaining Gateway vs Interface distinction (per D-04): Gateway endpoints are free and use route table entries; Interface endpoints cost ~$7/month and create ENIs in the subnet.
- VPC Endpoints table:
  | Service | Endpoint Type | Cost | Justification |
  |---|---|---|---|
  | Amazon S3 | Gateway | Free | Route-table based; all tiers access S3 for Data Lake and logs |
  | Amazon DynamoDB | Gateway | Free | Route-table based; Lambda in private subnet accesses device metadata |
  | Amazon Timestream | Interface | ~$7/month per AZ | ENI-based; Lambda queries time-series telemetry from private subnet |
  | AWS IoT Core Credential Provider | Interface | ~$7/month per AZ | ENI-based; Lambda invokes IoT Data Plane SDK (UpdateThingShadow) |
  | AWS Secrets Manager | Interface | ~$7/month per AZ | ENI-based; Lambda retrieves Aurora credentials at startup |
  | Amazon SQS | Interface | ~$7/month per AZ | ENI-based; Lambda sends to DLQ on processing failure |
  | Amazon Kinesis | Interface | ~$7/month per AZ | ENI-based; Lambda reads from Kinesis streams in private subnet |

**4. Security Groups subsection (`### Security Groups`):**
- Compact security group rules table:
  | Security Group | Inbound | Outbound | Attached To |
  |---|---|---|---|
  | sg-lambda | None (Lambda-initiated only) | TCP 5432 → sg-aurora; HTTPS 443 → VPC endpoints | Lambda functions in private subnet |
  | sg-aurora | TCP 5432 from sg-lambda | None required | Aurora cluster (data subnet) |
  | sg-alb | TCP 443 from 0.0.0.0/0 (HTTPS) | TCP 443 → sg-lambda (via VPC Link) | Application Load Balancer (public subnet) |
  | sg-vpce | TCP 443 from sg-lambda | None required | Interface VPC endpoints (data subnet) |
- Note: Gateway endpoints (S3, DynamoDB) do not require security groups — they use route table entries.

**5. Route Tables subsection (`### Route Tables`):**
- Three route tables:
  | Route Table | Destination | Target | Notes |
  |---|---|---|---|
  | rt-public | 0.0.0.0/0 | Internet Gateway | Public subnet internet access |
  | rt-public | 10.0.0.0/16 | local | VPC-internal traffic |
  | rt-private | 0.0.0.0/0 | NAT Gateway | Lambda outbound internet (AWS API calls) |
  | rt-private | 10.0.0.0/16 | local | VPC-internal traffic |
  | rt-private | pl-xxxxxxx (S3 prefix list) | vpce-s3 | S3 Gateway endpoint route |
  | rt-private | pl-xxxxxxx (DynamoDB prefix list) | vpce-dynamodb | DynamoDB Gateway endpoint route |
  | rt-data | 10.0.0.0/16 | local | VPC-internal only — NO internet route |
  | rt-data | pl-xxxxxxx (S3 prefix list) | vpce-s3 | S3 Gateway endpoint route |
  | rt-data | pl-xxxxxxx (DynamoDB prefix list) | vpce-dynamodb | DynamoDB Gateway endpoint route |
- Emphasize: data subnet has NO 0.0.0.0/0 route — databases cannot reach the internet.
  </action>
  <verify>
    <automated>grep -c "subgraph" docs/architecture/01-security-foundation.md | grep -q "[6-9]\|1[0-9]" && grep -q "Gateway endpoint" docs/architecture/01-security-foundation.md && grep -q "Interface endpoint" docs/architecture/01-security-foundation.md && grep -q "flowchart TD" docs/architecture/01-security-foundation.md && grep -q "sg-lambda" docs/architecture/01-security-foundation.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/architecture/01-security-foundation.md` exists
    - File contains `flowchart TD` Mermaid diagram
    - Diagram has at least 6 `subgraph` blocks (VPC, 3 tiers x 2 AZs minimum grouping)
    - File contains string "Gateway endpoint" at least twice (S3, DynamoDB)
    - File contains string "Interface endpoint" at least 3 times (Timestream, IoT Core Credential Provider, Secrets Manager, SQS, Kinesis)
    - File contains "| Subnet Name |" table header
    - File contains "| Service | Endpoint Type |" table header
    - File contains "sg-lambda" and "sg-aurora" in security groups table
    - File contains "rt-data" route table with NO 0.0.0.0/0 entry
    - File contains "10.0.0.0/16" CIDR reference
  </acceptance_criteria>
  <done>VPC topology section complete with 3-tier diagram (labeled subnets + AZs), VPC endpoints table (Gateway vs Interface explicitly typed), security groups table, and route tables showing data subnet has no internet route.</done>
</task>

<task type="auto">
  <name>Task 2: Write IAM roles inventory, KMS encryption, TLS, and WAF sections</name>
  <files>docs/architecture/01-security-foundation.md</files>
  <read_first>
    docs/architecture/01-security-foundation.md
    .planning/phases/01-foundation-device-connectivity-security/01-RESEARCH.md
  </read_first>
  <action>
Append the following sections to `docs/architecture/01-security-foundation.md` (after the VPC/route tables content from Task 1):

**1. IAM Least-Privilege Roles (`### IAM Least-Privilege Roles`):**
- Opening paragraph (per D-13, SEC-03): Every AWS service in the architecture assumes a dedicated IAM role scoped to the minimum permissions required. No service uses the root account or broad `*` permissions.
- IAM roles inventory table using the exact pattern from 01-RESEARCH.md Pattern 6. Columns: Role Name, Assumed By, Permissions, Scope. Include ALL of these rows:
  | Role Name | Assumed By | Permissions | Scope |
  |---|---|---|---|
  | IoTRulesTelemetryRole | IoT Rules Engine | `kinesis:PutRecord` | Specific Kinesis stream ARN |
  | IoTRulesAlarmRole | IoT Rules Engine | `lambda:InvokeFunction` | Specific Lambda function ARN |
  | IoTRulesConfigRole | IoT Rules Engine | `dynamodb:PutItem` | Specific DynamoDB config table ARN |
  | IoTRulesErrorRole | IoT Rules Engine | `sqs:SendMessage` | Specific SQS DLQ ARN |
  | LambdaAlarmRole | Lambda (alarm evaluator) | `sns:Publish`, `dynamodb:GetItem`, `dynamodb:PutItem` | Specific SNS topic ARN + DynamoDB table ARNs |
  | LambdaAPIRole | Lambda (API handlers) | `dynamodb:GetItem/PutItem/Query`, `timestream:Select` | Specific DynamoDB table + Timestream database ARNs |
  | LambdaShadowWriterRole | Lambda (command handler) | `iot:UpdateThingShadow` | `arn:aws:iot:{region}:{account}:thing/*` (thing ARN pattern) |
  | GlueETLRole | AWS Glue ETL jobs | `s3:GetObject`, `s3:PutObject`, `glue:GetTable` | Specific S3 bucket + Glue catalog ARNs |
  | FirehoseDeliveryRole | Kinesis Firehose | `s3:PutObject`, `s3:PutObjectAcl` | Specific S3 Data Lake bucket ARN |
  | CognitoAuthRole | Cognito Identity Pool (authenticated users) | `timestream:Select`, `s3:GetObject` | Scoped Timestream DB + S3 prefix |
- Closing note: "Each role's trust policy restricts `sts:AssumeRole` to the specific AWS service principal (e.g., `lambda.amazonaws.com`, `iot.amazonaws.com`)."

**2. KMS Encryption at Rest (`### Encryption at Rest (KMS)`):**
- Paragraph (per SEC-04): All data stores use AWS KMS customer-managed keys (CMKs) for encryption at rest. CMKs provide audit trail via CloudTrail and support automatic key rotation (annual).
- KMS encryption inventory table using the exact pattern from 01-RESEARCH.md Pattern 7:
  | Service | Encryption Type | Key | What It Protects |
  |---|---|---|---|
  | S3 (Data Lake) | SSE-KMS | Customer-managed CMK | Raw JSON and Parquet objects in bronze/silver/gold zones |
  | DynamoDB | Encryption at rest | Customer-managed CMK | Device metadata, command queue, config state |
  | Timestream | Storage encryption | Customer-managed CMK (magnetic store) | Time-series telemetry records |
  | Aurora PostgreSQL | Storage encryption | Customer-managed CMK | User accounts, roles, alert rule configurations |
  | Kinesis Data Streams | Server-side encryption | Customer-managed CMK | In-flight telemetry records in stream shards |
  | CloudTrail | SSE-KMS | Customer-managed CMK | API audit logs |
- Note: "A single KMS CMK can be shared across services via key policy grants, or separate CMKs per service can be used for finer-grained access control. For this architecture, one CMK per service category (storage, streaming, audit) is recommended."

**3. Encryption in Transit (`### Encryption in Transit (TLS)`):**
- Paragraph (per SEC-05): All communication uses TLS 1.2 or higher. IoT Core enforces TLS 1.2 minimum at the protocol level — devices connecting via MQTT on port 8883 perform mutual TLS authentication (mTLS) using X.509 device certificates. HTTPS endpoints (API Gateway, CloudFront) enforce TLS 1.2 via AWS-managed certificate policies.
- Bullet list of TLS enforcement points:
  - **IoT Core MQTT (port 8883):** Mutual TLS — device X.509 certificate + AWS server certificate. TLS 1.2 minimum enforced by IoT Core. No application-level configuration needed.
  - **API Gateway HTTPS:** TLS 1.2 enforced by default. ACM-managed certificate attached to custom domain.
  - **CloudFront HTTPS:** TLS 1.2 minimum security policy (`TLSv1.2_2021`). ACM certificate in us-east-1.
  - **Aurora PostgreSQL:** In-transit encryption via SSL/TLS enforced by `rds.force_ssl=1` parameter.
  - **VPC Endpoint traffic:** Interface endpoints use TLS — traffic between Lambda and Timestream/Secrets Manager is encrypted in transit within the AWS backbone.

**4. WAF Placement (`### Web Application Firewall (WAF)`):**
- Paragraph (per SEC-06): AWS WAF WebACLs are attached to two resources to protect all external-facing entry points.
- WAF attachment table:
  | WebACL | Attached To | Managed Rule Groups | Custom Rules |
  |---|---|---|---|
  | waf-cloudfront | CloudFront distribution (SPA) | AWSManagedRulesCommonRuleSet, AWSManagedRulesKnownBadInputsRuleSet | Rate limiting: 1000 requests/5min per IP |
  | waf-api | API Gateway HTTP API stage | AWSManagedRulesCommonRuleSet, AWSManagedRulesSQLiRuleSet | Rate limiting: 500 requests/5min per IP; geo-restriction (optional) |
- Note: IoT Core MQTT connections are NOT behind WAF — they are authenticated via X.509 mutual TLS, which provides equivalent protection against unauthorized access.
  </action>
  <verify>
    <automated>grep -q "| Role Name |" docs/architecture/01-security-foundation.md && grep -q "IoTRulesTelemetryRole" docs/architecture/01-security-foundation.md && grep -q "SSE-KMS" docs/architecture/01-security-foundation.md && grep -q "TLS 1.2" docs/architecture/01-security-foundation.md && grep -q "waf-cloudfront" docs/architecture/01-security-foundation.md && grep -q "waf-api" docs/architecture/01-security-foundation.md && grep -c "IoTRules\|Lambda\|Glue\|Firehose\|Cognito" docs/architecture/01-security-foundation.md | grep -q "[8-9]\|[1-9][0-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `| Role Name |` table header in IAM section
    - File contains all 10 IAM role names: IoTRulesTelemetryRole, IoTRulesAlarmRole, IoTRulesConfigRole, IoTRulesErrorRole, LambdaAlarmRole, LambdaAPIRole, LambdaShadowWriterRole, GlueETLRole, FirehoseDeliveryRole, CognitoAuthRole
    - File contains `SSE-KMS` string in KMS section
    - File contains `Customer-managed CMK` at least 5 times (one per service)
    - File contains `TLS 1.2` string at least 3 times
    - File contains `port 8883` for IoT Core MQTT
    - File contains `waf-cloudfront` and `waf-api` WebACL names
    - File contains `AWSManagedRulesCommonRuleSet` managed rule group name
    - File contains `### Encryption at Rest`, `### Encryption in Transit`, `### Web Application Firewall` headings
  </acceptance_criteria>
  <done>IAM roles inventory (10 roles with specific permissions and scopes), KMS encryption table (6 services with CMK details), TLS enforcement points (5 bullet items), and WAF placement (2 WebACLs with managed rules) are all documented with evaluator-ready specificity.</done>
</task>

</tasks>

<verification>
- `docs/architecture/01-security-foundation.md` exists and is valid Markdown
- VPC diagram renders as Mermaid (contains `flowchart TD` and at least 6 `subgraph` blocks)
- All 6 SEC requirements are addressed:
  - SEC-01: VPC topology with private subnets for databases (3-tier diagram + subnet table)
  - SEC-02: VPC endpoints with Gateway/Interface labels (endpoints table)
  - SEC-03: IAM least-privilege roles (10-row inventory table)
  - SEC-04: KMS encryption at rest (6-service encryption table)
  - SEC-05: TLS 1.2+ in transit (5 enforcement points)
  - SEC-06: WAF on CloudFront and API Gateway (2 WebACLs)
</verification>

<success_criteria>
- An evaluator reading `01-security-foundation.md` can identify every subnet tier, every VPC endpoint type, every IAM role, every KMS-encrypted service, every TLS enforcement point, and both WAF attachment points without ambiguity.
- No section uses vague language like "appropriate security" or "proper encryption" — every item is specific and verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-device-connectivity-security/01-01-SUMMARY.md`
</output>
